package com.thesis.smile.iota.responses.error;

public enum NetworkErrorType {

    REMOTE_NODE_ERROR,
    EXCHANGE_RATE_ERROR,
    NETWORK_ERROR,
    IOTA_COOL_NETWORK_ERROR,
    ACCESS_ERROR,
    INVALID_HASH_ERROR,
    KEY_REUSE_ERROR
}
